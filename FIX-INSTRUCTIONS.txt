LESRA FILMS - FIXED GALLERY PROJECT

Fixes included:
1. Gallery imports now use '@/app/albums' because albums.ts is inside app/.
2. Next.js updated from 15.5.23 to 15.5.26.
3. Development script uses Turbopack to avoid the Windows Watchpack/system-drive scan issue.
4. Existing next.config.ts image settings are preserved.

After replacing the project files:
  Remove-Item -Recurse -Force .next -ErrorAction SilentlyContinue
  Remove-Item -Recurse -Force node_modules -ErrorAction SilentlyContinue
  npm install
  npm run dev

Recommended Node.js for this project: Node 22 LTS.
