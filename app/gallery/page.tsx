import Link from 'next/link';
import { getAlbums } from '@/app/albums';
export const dynamic='force-dynamic';
export default async function Gallery(){const albums=await getAlbums();return <main><section className="page-hero"><div className="container"><div className="eyebrow">Photo Projects</div><h1>The <span className="gold">Gallery</span></h1><p>Browse project albums from Lesra Films.</p></div></section><section className="section"><div className="container">{albums.length?<div className="grid album-grid">{albums.map(a=><article className="card album" key={a.slug}><img src={a.cover} alt={a.name}/><div className="album-body"><h3>{a.name}</h3><p>{a.images.length} image{a.images.length===1?'':'s'}</p><Link className="album-link" href={`/gallery/${a.slug}`}>View album →</Link></div></article>)}</div>:<div className="empty">No photo albums found yet. Add an album folder to <b>public/projects/photos</b> and redeploy.</div>}</div></section></main>}
