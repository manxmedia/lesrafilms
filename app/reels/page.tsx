'use client';
import { useEffect, useState } from 'react';
import Link from 'next/link';
import { contact } from '@/lib';

type Video={id:string;title:string;published:string;url:string;thumbnail:string};
export default function Reels(){const [videos,setVideos]=useState<Video[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{fetch('/api/reels').then(r=>r.json()).then(d=>setVideos(d.videos||[])).finally(()=>setLoading(false))},[]);return <main><section className="page-hero"><div className="container"><div className="eyebrow">Moving images</div><h1>Video <span className="gold">Reels</span></h1><p>Explore Lesra Films video work directly from the YouTube channel. The page refreshes from the channel feed automatically.</p></div></section><section className="section"><div className="container">{loading?<div className="empty">Loading Lesra Films videos…</div>:videos.length?<div className="grid reels-grid">{videos.map(v=><article className="card reel" key={v.id}><iframe src={`https://www.youtube.com/embed/${v.id}`} title={v.title} allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowFullScreen/><div className="reel-body"><h3>{v.title}</h3></div></article>)}</div>:<div className="empty">The YouTube feed is temporarily unavailable. <a className="gold" href={contact.youtube} target="_blank" rel="noreferrer">Open the Lesra Films channel →</a></div>}</div></section><section className="section" style={{paddingTop:0}}><div className="container"><div className="cta"><div><div className="eyebrow">YouTube</div><h2>See the complete channel.</h2><p>For the full Lesra Films video library, including uploads not present in the RSS feed, open the channel on YouTube.</p></div><a className="btn" href={contact.youtube} target="_blank" rel="noreferrer">Open YouTube →</a></div></div></section></main>}
